<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}الرئيسية{% endblock %}</title>
</head>
<body>
  {% block content %}
  <h1>مرحباً بكم</h1>
  <ul>
    {% for user in users %}
      <li>{{ user }}</li>
    {% endfor %}
  </ul>
  <p>عدد المستخدمين: {{ count }}</p>
  {% endblock %}
</body>
</html>
